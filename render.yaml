services:
  # 1. Backend (FastAPI Web Service)
  - type: web
    name: strat-edge-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    rootDir: backend
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: GCP_PROJECT_ID
        value: custom-mix-428318-h3
      - key: GCP_BUCKET_NAME
        value: stratedgestore
      - key: GOOGLE_APPLICATION_CREDENTIALS
        value: /etc/secrets/gcp-credentials.json

  # 2. Frontend (React Static Site)
  # We use 'runtime: static' within a web service or 'staticSites'
  # If 'staticSites' failed previously, we use 'type: web' + 'runtime: static'
  - type: web
    name: strat-edge-portal
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    rootDir: frontend
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://strat-edge-api.onrender.com
